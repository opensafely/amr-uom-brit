---
title: "data preparation: measure antibiotics exposure"
output: 
  html_document:
   toc: true
   toc_depth: 2
   toc_float:
     collapsed: false
     smooth_scorll: false
---

```{r setup, include=FALSE}
library('tidyverse')
library("ggplot2")
library('dplyr')
library('lubridate')
```

input matched patient ID
```{r}
#setwd('/Users/yayang/Documents/GitHub/amr-uom-brit/output')

df1=readRDS(here::here("output","matched_patients_general.rds"))
#df2=read.csv(here::here("output","matched_patients_general.rds"))
nrow(df1)


df1=df1%>%filter(cal_YM=="2020-12")
nrow(df1)

df2=df1%>%distinct(patient_id,patient_index_date)
nrow(df2)


df3=df1%>%distinct(patient_id)
nrow(df3)

df=df1%>%filter(case=="1")
nrow(df)

# 2020-03-01

```

```{r}

DF=read.csv(here::here("output","control_covid_infection.csv"))
nrow(DF)

DF=DF%>% filter(cal_YM=="2020-12")
nrow(DF)

DF2=DF%>%distinct(patient_id,patient_index_date)
nrow(DF2)


DF3=DF%>%distinct(patient_id)
nrow(DF3)
```

