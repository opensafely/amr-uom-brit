---
title: "classification comparison: RF "
---

```{r setup, include=FALSE}
library("survival")
library('tidyverse')
library(pROC) #plyr
library(caret) #dplyr
library(randomForest)
```

# import dataset
```{r }
train <- read_rds(here::here("output","train_cat.rds"))
train$case=as.factor(train$case)

#valid <- read_rds(here::here("output","valid_cat.rds"))
#valid$case=as.factor(valid$case)

table(train$case)
#table(valid$case)
```



# pick variable
```{r}
names(train)
col=c("case","total_ab_group","ab_types_group",
      "exposure_period_group","recent_ab_days_group",
      "broad_ab_prescriptions_group",
      "interval_med_group" ,"interval_sd_group","age.x")
#"broad_prop_total"

data=train[col]
str(data)


```

# rename variable
```{r}
data$case=ifelse(data$case==1,"case","control")
table(data$case)

for (i in 2:length(col)) {
data[,paste0(col[i])]=ifelse(data[,paste0(col[i])]==0,paste0(col[i],"_0"),data[,paste0(col[i])])
data[,paste0(col[i])]=ifelse(data[,paste0(col[i])]==1,paste0(col[i],"_1"),data[,paste0(col[i])])
data[,paste0(col[i])]=ifelse(data[,paste0(col[i])]==2,paste0(col[i],"_2"),data[,paste0(col[i])])
data[,paste0(col[i])]=ifelse(data[,paste0(col[i])]==3,paste0(col[i],"_3"),data[,paste0(col[i])])
data[,paste0(col[i])]=ifelse(data[,paste0(col[i])]==4,paste0(col[i],"_4"),data[,paste0(col[i])])

print(col[i])
print(prop.table(table(data[,paste0(col[i])]))*100)
}
```



# matched 
http://www.sthda.com/english/articles/35-statistical-machine-learning-essentials/141-cart-model-decision-tree-essentials/
```{r }
set.seed(1024)
model <-train(case ~ ., 
                  data=data, method="rpart",
                  trControl = trainControl("cv", number = 5),
                  maxdepth= 5)
  
print(model)

```


```{r }
model$finalModel
```

# Plot the final tree model
```{r }
try(par(xpd = NA)) # Avoid clipping the text in some device
try(plot(model$finalModel))
try(text(model$finalModel,  digits = 3))

```


```{r eval=FALSE, include=FALSE}
library(rpart)
rf.model<- rpart(
  case ~ ., 
  data = data, 
  method = "class"
 # minsplit = 2,  minbucket = 1
  )

print(rf.model)
par(xpd = NA)
plot(rf.model)
text(rf.model,  digits = 3)
```


```{r eval=FALSE, include=FALSE}
rf.model <- randomForest(formula = case ~ ., data = data, 
                           replace = TRUE,
                           ntree =  1,
                           sampsize = round(nrow(data)*0.6),
                           nodesize = 1 ,
                           mtry = sqrt(ncol(data)-1)
                          )
  print(rf.model)
```


```{r eval=FALSE, include=FALSE}
getTree(rf.model, k=1, labelVar=TRUE)


```


# filter ab users (unmatched)
```{r}

data=train%>% distinct(patient_id,.keep_all = TRUE) %>%filter(total_ab>0)
data=data[col]
```

```{r }
set.seed(1024)
#for (i in 1:nrow(result)) {

data$case=as.factor(data$case)  

model <-train(case ~ ., 
                  data=data, method="rpart",
                  trControl = trainControl("cv", number = 5),
                  maxdepth= 5)
  
print(model)
#  plot(rf.model)
  #  rf.model$bestTune
```


```{r }
model$finalModel
```

# Plot the final tree model
```{r }
try(par(xpd = NA)) # Avoid clipping the text in some device
try(plot(model$finalModel))
try(text(model$finalModel,  digits = 3))
```
